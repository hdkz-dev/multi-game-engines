{
  "name": "@multi-game-engines/cdn-worker",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && echo 'No build step' > dist/.placeholder",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260228.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.67.0",
    "@multi-game-engines/core": "workspace:*"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
