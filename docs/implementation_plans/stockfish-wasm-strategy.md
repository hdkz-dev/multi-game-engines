# Stockfish WASM 配信戦略: ベストプラクティス設計

> 作成日: 2026-02-06
> ステータス: 承認待ち

## 📋 エグゼクティブサマリー

### 🎯 目標

1. **全パッケージ MIT ライセンス化** - ユーザーにライセンス懸念を与えない
2. **ゼロコスト配信** - 自前インフラ構築不要
3. **即座に利用可能** - 既存の成熟したエコシステムを活用
4. **フォールバック対応** - 単一障害点を排除

### 🏆 推奨アプローチ

**Phase 1 (即時)**: 既存 npm パッケージ (`stockfish@17.1.0`) を jsDelivr/unpkg 経由で配信
**Phase 2 (将来)**: 必要に応じて Cloudflare R2 による自前 CDN を構築

---

## 📊 調査結果サマリー

### 既存パッケージ評価

| パッケージ                   | 週間DL | 初版 | 最新SF版 | ライセンス   | メンテナンス  | 推奨    |
| ---------------------------- | ------ | ---- | -------- | ------------ | ------------- | ------- |
| `stockfish`                  | 7,883  | 2014 | **17.1** | GPL-3.0      | ✅ アクティブ | **🥇**  |
| `stockfish.wasm`             | 942    | 2018 | ~15      | GPL-3.0      | ⚠️ 停滞       | 🥈      |
| `@lichess-org/stockfish-web` | 730    | 2025 | 最新     | **AGPL-3.0** | ✅ アクティブ | ❌ AGPL |

### CDN コスト比較

| サービス          | 月間コスト | 転送量上限 | 設定難易度 |
| ----------------- | ---------- | ---------- | ---------- |
| **jsDelivr**      | $0         | 無制限     | ゼロ       |
| **unpkg**         | $0         | 無制限     | ゼロ       |
| **Cloudflare R2** | $0         | 無制限     | 中         |
| GitHub Releases   | $0         | 制限あり   | 低         |

---

## 🏗️ ベストプラクティス設計

### アーキテクチャ概要

```text
┌─────────────────────────────────────────────────────────────────────┐
│                        ユーザーアプリケーション                      │
│                              (MIT)                                   │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   @multi-game-engines/adapter-stockfish             │
│                              (MIT)                                   │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ EngineSourceConfig                                          │    │
│  │  - primary: jsDelivr (stockfish@17.1.0)                     │    │
│  │  - fallback: unpkg (stockfish@17.1.0)                       │    │
│  │  - custom: ユーザー指定URL                                  │    │
│  └─────────────────────────────────────────────────────────────┘    │
└───────────────────────────────┬─────────────────────────────────────┘
                                │ 動的ロード (実行時)
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     外部 CDN (GPL エンジンバイナリ)                  │
│                                                                     │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │    jsDelivr      │  │     unpkg        │  │  カスタム CDN    │  │
│  │   (Primary)      │  │   (Fallback)     │  │   (Optional)     │  │
│  │  stockfish.wasm  │  │  stockfish.wasm  │  │  stockfish.wasm  │  │
│  │  stockfish.js    │  │  stockfish.js    │  │  stockfish.js    │  │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘  │
│                              (GPL-3.0)                              │
└─────────────────────────────────────────────────────────────────────┘
```

### ライセンス分離

```text
┌─────────────────────────────────────────┐
│         MIT ライセンス領域              │
│                                         │
│  • @multi-game-engines/core             │
│  • @multi-game-engines/adapter-*        │
│  • @multi-game-engines/protocols        │
│  • ユーザーアプリケーション             │
│                                         │
└─────────────────────────────────────────┘
                    │
                    │ 実行時ロード (GPL 感染なし)
                    ▼
┌─────────────────────────────────────────┐
│         GPL-3.0 ライセンス領域          │
│                                         │
│  • stockfish.wasm (バイナリ)            │
│  • stockfish.js (ローダー)              │
│                                         │
│  ※ 外部 CDN から配信                    │
│  ※ アダプターに同梱しない               │
│                                         │
└─────────────────────────────────────────┘
```

---

## 📦 実装詳細

### 1. デフォルトソース設定

```typescript
// packages/adapter-stockfish/src/sources.ts

export interface EngineSource {
  /** JavaScript ローダーの URL */
  js: string;
  /** WASM バイナリの URL */
  wasm: string;
  /** SRI ハッシュ (オプション) */
  sri?: {
    js?: string;
    wasm?: string;
  };
}

export interface EngineSourceConfig {
  /** プライマリソース */
  primary: EngineSource;
  /** フォールバックソース */
  fallback?: EngineSource;
  /** カスタムソース (ユーザー指定) */
  custom?: EngineSource;
}

/**
 * デフォルトソース設定
 *
 * 優先順位:
 * 1. custom (ユーザー指定)
 * 2. primary (jsDelivr)
 * 3. fallback (unpkg)
 */
export const DEFAULT_SOURCES: EngineSourceConfig = {
  primary: {
    js: "https://cdn.jsdelivr.net/npm/stockfish@17.1.0/src/stockfish-17.1.js",
    wasm: "https://cdn.jsdelivr.net/npm/stockfish@17.1.0/src/stockfish-17.1.wasm",
  },
  fallback: {
    js: "https://unpkg.com/stockfish@17.1.0/src/stockfish-17.1.js",
    wasm: "https://unpkg.com/stockfish@17.1.0/src/stockfish-17.1.wasm",
  },
};

/**
 * Lite バージョン (モバイル向け、約7MB)
 */
export const LITE_SOURCES: EngineSourceConfig = {
  primary: {
    js: "https://cdn.jsdelivr.net/npm/stockfish@17.1.0/src/stockfish-17.1-lite.js",
    wasm: "https://cdn.jsdelivr.net/npm/stockfish@17.1.0/src/stockfish-17.1-lite.wasm",
  },
  fallback: {
    js: "https://unpkg.com/stockfish@17.1.0/src/stockfish-17.1-lite.js",
    wasm: "https://unpkg.com/stockfish@17.1.0/src/stockfish-17.1-lite.wasm",
  },
};
```

### 2. 動的ローダー

```typescript
// packages/adapter-stockfish/src/loader.ts

import { EngineSourceConfig, DEFAULT_SOURCES } from "./sources";

export interface LoaderOptions {
  sources?: Partial<EngineSourceConfig>;
  timeout?: number;
  validateSRI?: boolean;
}

export class StockfishLoader {
  private config: EngineSourceConfig;
  private timeout: number;

  constructor(options: LoaderOptions = {}) {
    this.config = {
      ...DEFAULT_SOURCES,
      ...options.sources,
    };
    this.timeout = options.timeout ?? 30000;
  }

  async load(): Promise<Worker> {
    // 1. カスタムソースを優先
    if (this.config.custom) {
      try {
        return await this.loadFromSource(this.config.custom);
      } catch (error) {
        console.warn("Custom source failed, trying primary:", error);
      }
    }

    // 2. プライマリソース (jsDelivr)
    try {
      return await this.loadFromSource(this.config.primary);
    } catch (error) {
      console.warn("Primary source failed, trying fallback:", error);
    }

    // 3. フォールバックソース (unpkg)
    if (this.config.fallback) {
      return await this.loadFromSource(this.config.fallback);
    }

    throw new Error("All engine sources failed");
  }

  private async loadFromSource(source: EngineSource): Promise<Worker> {
    // Web Worker を作成
    const workerBlob = await this.fetchWithTimeout(source.js, source.sri?.js);
    const workerUrl = URL.createObjectURL(workerBlob);

    try {
      const worker = new Worker(workerUrl, { type: "module" });

      // WASM の場所を通知
      worker.postMessage({ type: "init", wasmUrl: source.wasm });

      return worker;
    } finally {
      // Blob URL を解放してメモリリークを防ぐ
      URL.revokeObjectURL(workerUrl);
    }
  }

  private async fetchWithTimeout(
    url: string,
    integrity?: string,
  ): Promise<Blob> {
    const controller = new AbortController();
    const timeoutId = setTimeout(() => controller.abort(), this.timeout);

    try {
      // SRI ハッシュがある場合は integrity オプションを指定
      const options: RequestInit = {
        signal: controller.signal,
        integrity: integrity,
      };

      const response = await fetch(url, options);
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      return await response.blob();
    } finally {
      clearTimeout(timeoutId);
    }
  }
}
```

### 3. アダプター公開 API

```typescript
// packages/adapter-stockfish/src/index.ts

export { StockfishAdapter } from "./adapter";
export { StockfishLoader } from "./loader";
export { DEFAULT_SOURCES, LITE_SOURCES } from "./sources";
export type {
  EngineSource,
  EngineSourceConfig,
  LoaderOptions,
} from "./sources";

// 使用例を README に記載
```

---

## 📋 実装計画

### Phase 1: 基本実装 (Week 1-2)

| タスク                                  | 優先度 | 工数 | 担当    |
| --------------------------------------- | ------ | ---- | ------- |
| `sources.ts` - ソース設定定義           | 🔴 高  | 2h   | -       |
| `loader.ts` - 動的ローダー実装          | 🔴 高  | 4h   | -       |
| `adapter.ts` - UCI プロトコルハンドラー | 🔴 高  | 8h   | -       |
| ユニットテスト作成                      | 🟡 中  | 4h   | -       |
| README.md 更新                          | 🟡 中  | 2h   | -       |
| package.json ライセンス更新             | 🔴 高  | 0.5h | ✅ 完了 |

### Phase 2: 高度な機能 (Week 3-4)

| タスク                                 | 優先度 | 工数 | 担当 |
| -------------------------------------- | ------ | ---- | ---- |
| SRI ハッシュ検証                       | 🟡 中  | 4h   | -    |
| マルチスレッド対応 (SharedArrayBuffer) | 🟡 中  | 8h   | -    |
| Lite バージョン自動選択 (デバイス検出) | 🟢 低  | 4h   | -    |
| E2E テスト作成                         | 🟡 中  | 4h   | -    |

### Phase 3: 自前 CDN (オプション、将来)

| タスク                     | 優先度 | 工数 | 担当 |
| -------------------------- | ------ | ---- | ---- |
| Cloudflare R2 バケット作成 | 🟢 低  | 2h   | -    |
| Worker デプロイ            | 🟢 低  | 2h   | -    |
| マニフェスト生成スクリプト | 🟢 低  | 4h   | -    |
| CI/CD パイプライン         | 🟢 低  | 4h   | -    |

---

## ✅ チェックリスト

### 設計原則

- [x] **MIT ライセンス**: アダプターは MIT、エンジンは外部配信
- [x] **ゼロコスト**: jsDelivr/unpkg による無料配信
- [x] **フォールバック**: 複数 CDN による冗長化
- [x] **型安全性**: TypeScript 完全対応
- [x] **疎結合**: エンジンバイナリはアダプターに同梱しない

### セキュリティ

- [ ] SRI ハッシュ検証 (Phase 2)
- [ ] CORS ヘッダー確認
- [ ] CSP 対応ドキュメント

### パフォーマンス

- [ ] Lite バージョン自動選択
- [ ] マルチスレッド対応
- [ ] WASM ストリーミングコンパイル

---

## 📄 関連ドキュメント

| ドキュメント                                                                                 | 内容                 |
| -------------------------------------------------------------------------------------------- | -------------------- |
| [`EXISTING_PACKAGES.md`](./EXISTING_PACKAGES.md)                                             | 既存パッケージ調査   |
| [`CDN_COMPARISON.md`](./CDN_COMPARISON.md)                                                   | CDN コスト比較       |
| [`mit-license-architecture.md`](../../docs/implementation_plans/mit-license-architecture.md) | MIT ライセンス化設計 |
| [`DECISION_LOG.md`](../../docs/DECISION_LOG.md)                                              | ADR-014              |

---

## 🎯 結論

### 推奨アプローチ

1. **既存パッケージ `stockfish@17.1.0` を活用**
   - 11年の実績、Chess.com 協力
   - Stockfish 17.1 (最新) 対応
   - jsDelivr/unpkg で無料配信済み

2. **自前 CDN は不要 (当面)**
   - 既存インフラで十分
   - 必要になった時点で Cloudflare R2 を検討

3. **MIT ライセンス化を実現**
   - アダプターはプロトコルハンドラーのみ (MIT)
   - エンジンバイナリは外部 CDN から動的ロード (GPL)

### 即座に開始可能なタスク

```bash
# 1. パッケージ作成 (MIT ライセンス)
cd packages/adapter-stockfish
npm init

# 2. ソース設定実装
touch src/sources.ts src/loader.ts src/adapter.ts

# 3. テスト実行
npm test
```

**この設計により、ゼロコスト・ゼロインフラで即座に Stockfish 統合が可能になります。**
